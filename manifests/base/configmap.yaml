apiVersion: v1
kind: ConfigMap
metadata:
  name: lamp-config
  labels:
    app: lamp-app
data:
  # Application configuration key-value pairs
  APP_ENV: "production"
  APP_DEBUG: "false"
  # Add other configuration variables as needed